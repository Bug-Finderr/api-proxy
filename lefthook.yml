# Apply only safe Biome fixes to staged files.
pre-commit:
  commands:
    check:
      glob: "*.{js,ts,cjs,mjs,mts,d.cts,d.mts,jsx,tsx,json,jsonc,css}"
      run: nub exec biome check --write --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
      stage_fixed: true
